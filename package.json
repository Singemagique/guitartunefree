{
  "name": "guitartunefree",
  "private": true,
  "version": "2.1.0",
  "description": "TrueString — a free, lightweight guitar tuner and metronome. Auto tuner, manual tuner, tuning presets. Web + Android.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "icons": "node scripts/make-icons.mjs",
    "cap:sync": "npm run build && npx cap sync android"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0",
    "sharp": "^0.35.3",
    "typescript": "~5.6.3",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  }
}
